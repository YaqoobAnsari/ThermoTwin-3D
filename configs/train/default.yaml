epochs: 200
batch_size: 16
lr: 1.0e-3
weight_decay: 1.0e-5
optimizer: adamw
scheduler: cosine
grad_clip: 1.0
log_every: 50
ckpt_every: 10
physics_weight: 0.0        # >0 adds physics_weight * steady-FV PDE residual loss
